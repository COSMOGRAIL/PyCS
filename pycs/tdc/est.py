"""

"""

import pycs.gen.lc
import numpy as np


class estimate:
	"""
	Class to hold delay estimates for TDC curves, as obtained by various delay estimators.
	"""
	
	def __init__(self, set="tdc0", rung=0, pair=1, td=0.0, tderr=0.0, ms=0.0):
		"""

		"""
	
		self.set = set
		self.rung = rung
		self.pair = pair
		self.td = td
		self.tderr = tderr
		self.ms = ms



	def __str__(self):
		return "Season %s [%i, %i]" % (self.name, self.indices[0], self.indices[-1])
	
	

importd3cs(filepath='d3cslog.txt', user='vivien-08/11', maxconflevel=4, writeto=None):	

		
	
